package com.fcms.naveen.repository;

import com.fcms.naveen.model.NaveenSupplier;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NaveenSupplierRepository extends JpaRepository<NaveenSupplier, Long> {
}
